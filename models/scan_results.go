// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/queries"
	"github.com/vattle/sqlboiler/queries/qm"
	"github.com/vattle/sqlboiler/strmangle"
	"github.com/vattle/sqlboiler/types"
	"gopkg.in/nullbio/null.v6"
)

// ScanResult is an object representing the database table.
type ScanResult struct {
	ID         int               `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt  null.Time         `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt  null.Time         `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	DeletedAt  null.Time         `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	ScannedAt  null.Time         `boil:"scanned_at" json:"scanned_at,omitempty" toml:"scanned_at" yaml:"scanned_at,omitempty"`
	Lang       null.String       `boil:"lang" json:"lang,omitempty" toml:"lang" yaml:"lang,omitempty"`
	ServerName null.String       `boil:"server_name" json:"server_name,omitempty" toml:"server_name" yaml:"server_name,omitempty"`
	Family     null.String       `boil:"family" json:"family,omitempty" toml:"family" yaml:"family,omitempty"`
	Release    null.String       `boil:"release" json:"release,omitempty" toml:"release" yaml:"release,omitempty"`
	Errors     types.StringArray `boil:"errors" json:"errors,omitempty" toml:"errors" yaml:"errors,omitempty"`

	R *scanResultR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L scanResultL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

// scanResultR is where relationships are stored.
type scanResultR struct {
}

// scanResultL is where Load methods for each relationship are stored.
type scanResultL struct{}

var (
	scanResultColumns               = []string{"id", "created_at", "updated_at", "deleted_at", "scanned_at", "lang", "server_name", "family", "release", "errors"}
	scanResultColumnsWithoutDefault = []string{"created_at", "updated_at", "deleted_at", "scanned_at", "lang", "server_name", "family", "release", "errors"}
	scanResultColumnsWithDefault    = []string{"id"}
	scanResultPrimaryKeyColumns     = []string{"id"}
)

type (
	// ScanResultSlice is an alias for a slice of pointers to ScanResult.
	// This should generally be used opposed to []ScanResult.
	ScanResultSlice []*ScanResult
	// ScanResultHook is the signature for custom ScanResult hook methods
	ScanResultHook func(boil.Executor, *ScanResult) error

	scanResultQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	scanResultType                 = reflect.TypeOf(&ScanResult{})
	scanResultMapping              = queries.MakeStructMapping(scanResultType)
	scanResultPrimaryKeyMapping, _ = queries.BindMapping(scanResultType, scanResultMapping, scanResultPrimaryKeyColumns)
	scanResultInsertCacheMut       sync.RWMutex
	scanResultInsertCache          = make(map[string]insertCache)
	scanResultUpdateCacheMut       sync.RWMutex
	scanResultUpdateCache          = make(map[string]updateCache)
	scanResultUpsertCacheMut       sync.RWMutex
	scanResultUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var scanResultBeforeInsertHooks []ScanResultHook
var scanResultBeforeUpdateHooks []ScanResultHook
var scanResultBeforeDeleteHooks []ScanResultHook
var scanResultBeforeUpsertHooks []ScanResultHook

var scanResultAfterInsertHooks []ScanResultHook
var scanResultAfterSelectHooks []ScanResultHook
var scanResultAfterUpdateHooks []ScanResultHook
var scanResultAfterDeleteHooks []ScanResultHook
var scanResultAfterUpsertHooks []ScanResultHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ScanResult) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ScanResult) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ScanResult) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ScanResult) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ScanResult) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ScanResult) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ScanResult) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ScanResult) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ScanResult) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range scanResultAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddScanResultHook registers your hook function for all future operations.
func AddScanResultHook(hookPoint boil.HookPoint, scanResultHook ScanResultHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		scanResultBeforeInsertHooks = append(scanResultBeforeInsertHooks, scanResultHook)
	case boil.BeforeUpdateHook:
		scanResultBeforeUpdateHooks = append(scanResultBeforeUpdateHooks, scanResultHook)
	case boil.BeforeDeleteHook:
		scanResultBeforeDeleteHooks = append(scanResultBeforeDeleteHooks, scanResultHook)
	case boil.BeforeUpsertHook:
		scanResultBeforeUpsertHooks = append(scanResultBeforeUpsertHooks, scanResultHook)
	case boil.AfterInsertHook:
		scanResultAfterInsertHooks = append(scanResultAfterInsertHooks, scanResultHook)
	case boil.AfterSelectHook:
		scanResultAfterSelectHooks = append(scanResultAfterSelectHooks, scanResultHook)
	case boil.AfterUpdateHook:
		scanResultAfterUpdateHooks = append(scanResultAfterUpdateHooks, scanResultHook)
	case boil.AfterDeleteHook:
		scanResultAfterDeleteHooks = append(scanResultAfterDeleteHooks, scanResultHook)
	case boil.AfterUpsertHook:
		scanResultAfterUpsertHooks = append(scanResultAfterUpsertHooks, scanResultHook)
	}
}

// OneP returns a single scanResult record from the query, and panics on error.
func (q scanResultQuery) OneP() *ScanResult {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single scanResult record from the query.
func (q scanResultQuery) One() (*ScanResult, error) {
	o := &ScanResult{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for scan_results")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all ScanResult records from the query, and panics on error.
func (q scanResultQuery) AllP() ScanResultSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all ScanResult records from the query.
func (q scanResultQuery) All() (ScanResultSlice, error) {
	var o []*ScanResult

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ScanResult slice")
	}

	if len(scanResultAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all ScanResult records in the query, and panics on error.
func (q scanResultQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all ScanResult records in the query.
func (q scanResultQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count scan_results rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q scanResultQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q scanResultQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if scan_results exists")
	}

	return count > 0, nil
}

// ScanResultsG retrieves all records.
func ScanResultsG(mods ...qm.QueryMod) scanResultQuery {
	return ScanResults(boil.GetDB(), mods...)
}

// ScanResults retrieves all the records using an executor.
func ScanResults(exec boil.Executor, mods ...qm.QueryMod) scanResultQuery {
	mods = append(mods, qm.From("\"scan_results\""))
	return scanResultQuery{NewQuery(exec, mods...)}
}

// FindScanResultG retrieves a single record by ID.
func FindScanResultG(id int, selectCols ...string) (*ScanResult, error) {
	return FindScanResult(boil.GetDB(), id, selectCols...)
}

// FindScanResultGP retrieves a single record by ID, and panics on error.
func FindScanResultGP(id int, selectCols ...string) *ScanResult {
	retobj, err := FindScanResult(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindScanResult retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindScanResult(exec boil.Executor, id int, selectCols ...string) (*ScanResult, error) {
	scanResultObj := &ScanResult{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"scan_results\" where \"id\"=$1", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(scanResultObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from scan_results")
	}

	return scanResultObj, nil
}

// FindScanResultP retrieves a single record by ID with an executor, and panics on error.
func FindScanResultP(exec boil.Executor, id int, selectCols ...string) *ScanResult {
	retobj, err := FindScanResult(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *ScanResult) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *ScanResult) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *ScanResult) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *ScanResult) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no scan_results provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.Time.IsZero() {
		o.CreatedAt.Time = currTime
		o.CreatedAt.Valid = true
	}
	if o.UpdatedAt.Time.IsZero() {
		o.UpdatedAt.Time = currTime
		o.UpdatedAt.Valid = true
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(scanResultColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	scanResultInsertCacheMut.RLock()
	cache, cached := scanResultInsertCache[key]
	scanResultInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			scanResultColumns,
			scanResultColumnsWithDefault,
			scanResultColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(scanResultType, scanResultMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(scanResultType, scanResultMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"scan_results\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"scan_results\" DEFAULT VALUES"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into scan_results")
	}

	if !cached {
		scanResultInsertCacheMut.Lock()
		scanResultInsertCache[key] = cache
		scanResultInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single ScanResult record. See Update for
// whitelist behavior description.
func (o *ScanResult) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single ScanResult record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *ScanResult) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the ScanResult, and panics on error.
// See Update for whitelist behavior description.
func (o *ScanResult) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the ScanResult.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *ScanResult) Update(exec boil.Executor, whitelist ...string) error {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt.Time = currTime
	o.UpdatedAt.Valid = true

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	scanResultUpdateCacheMut.RLock()
	cache, cached := scanResultUpdateCache[key]
	scanResultUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			scanResultColumns,
			scanResultPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update scan_results, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"scan_results\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, scanResultPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(scanResultType, scanResultMapping, append(wl, scanResultPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update scan_results row")
	}

	if !cached {
		scanResultUpdateCacheMut.Lock()
		scanResultUpdateCache[key] = cache
		scanResultUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q scanResultQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q scanResultQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for scan_results")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o ScanResultSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o ScanResultSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o ScanResultSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ScanResultSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), scanResultPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"scan_results\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, scanResultPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in scanResult slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *ScanResult) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *ScanResult) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *ScanResult) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *ScanResult) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no scan_results provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.Time.IsZero() {
		o.CreatedAt.Time = currTime
		o.CreatedAt.Valid = true
	}
	o.UpdatedAt.Time = currTime
	o.UpdatedAt.Valid = true

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(scanResultColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()

	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	scanResultUpsertCacheMut.RLock()
	cache, cached := scanResultUpsertCache[key]
	scanResultUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			scanResultColumns,
			scanResultColumnsWithDefault,
			scanResultColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			scanResultColumns,
			scanResultPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert scan_results, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(scanResultPrimaryKeyColumns))
			copy(conflict, scanResultPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"scan_results\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(scanResultType, scanResultMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(scanResultType, scanResultMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert scan_results")
	}

	if !cached {
		scanResultUpsertCacheMut.Lock()
		scanResultUpsertCache[key] = cache
		scanResultUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single ScanResult record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ScanResult) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single ScanResult record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *ScanResult) DeleteG() error {
	if o == nil {
		return errors.New("models: no ScanResult provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single ScanResult record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ScanResult) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single ScanResult record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ScanResult) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no ScanResult provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), scanResultPrimaryKeyMapping)
	sql := "DELETE FROM \"scan_results\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from scan_results")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q scanResultQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q scanResultQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no scanResultQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from scan_results")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o ScanResultSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o ScanResultSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no ScanResult slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o ScanResultSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ScanResultSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no ScanResult slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(scanResultBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), scanResultPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"scan_results\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, scanResultPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from scanResult slice")
	}

	if len(scanResultAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *ScanResult) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *ScanResult) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *ScanResult) ReloadG() error {
	if o == nil {
		return errors.New("models: no ScanResult provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ScanResult) Reload(exec boil.Executor) error {
	ret, err := FindScanResult(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ScanResultSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ScanResultSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ScanResultSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty ScanResultSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ScanResultSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	scanResults := ScanResultSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), scanResultPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"scan_results\".* FROM \"scan_results\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, scanResultPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&scanResults)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ScanResultSlice")
	}

	*o = scanResults

	return nil
}

// ScanResultExists checks if the ScanResult row exists.
func ScanResultExists(exec boil.Executor, id int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"scan_results\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if scan_results exists")
	}

	return exists, nil
}

// ScanResultExistsG checks if the ScanResult row exists.
func ScanResultExistsG(id int) (bool, error) {
	return ScanResultExists(boil.GetDB(), id)
}

// ScanResultExistsGP checks if the ScanResult row exists. Panics on error.
func ScanResultExistsGP(id int) bool {
	e, err := ScanResultExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ScanResultExistsP checks if the ScanResult row exists. Panics on error.
func ScanResultExistsP(exec boil.Executor, id int) bool {
	e, err := ScanResultExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
